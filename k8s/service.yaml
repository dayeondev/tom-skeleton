apiVersion: v1
kind: Service
metadata:
  name: tom-skeleton-service
  labels:
    app: tom-skeleton
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: tom-skeleton